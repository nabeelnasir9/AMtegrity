import { Grid } from '@mui/material';
import React from 'react';

const Scope = () => {
    return (
        <Grid container sx={{mt:''}}>
            <Grid item>reds</Grid>

            </Grid>
    );
};

export default Scope;